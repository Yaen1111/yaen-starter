<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd 
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd 
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- allow annotation like @Service -->
	<context:annotation-config />

	<!-- scan for component like @Service -->
	<context:component-scan base-package="org.yaen.starter.biz.service" />

	<!-- dubbo name -->
	<dubbo:application name="starterDubboProvider" />

	<!-- dubbo registry server address, allow multi cast -->
	<dubbo:registry address="${registry.address}" />

	<!-- this is dubbo provider -->
	<dubbo:provider timeout="6000" />

	<!-- dubbo provider local port -->
	<dubbo:protocol name="dubbo" port="${dubbo.port}" />


	<!-- the follow is dubbo service -->

	<!-- handshake service -->
	<bean id="HandshakeService" class="org.yaen.starter.biz.service.impl.HandshakeServiceImpl" />
	<dubbo:service interface="org.yaen.starter.common.facade.HandshakeService" ref="HandshakeService" />

</beans>