<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.yaen.starter.common.dal.mappers.QueryMapper">

	<select id="selectIDsByColumns" resultType="java.lang.Long" parameterType="org.yaen.starter.common.dal.entities.QueryEntity">
		SELECT
		`ID`
		FROM `${tableName}`
		<where>
			<foreach collection="columns" index="index" item="info" open="" separator="AND" close="">
				`${index}` = #{info}
			</foreach>
		</where>
	</select>

	<select id="selectIDsByWhereClause" resultType="java.lang.Long" parameterType="org.yaen.starter.common.dal.entities.QueryEntity">
		SELECT
		`ID`
		FROM `${tableName}`
		<where>
			${whereClause}
		</where>
	</select>

</mapper>