<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd"
	default-autowire="byName">

	<!-- web using jsp template -->

	<!-- allow annotation like @Service, only in this file -->
	<context:annotation-config />

	<!-- scan for component like @Service, only in this file -->
	<context:component-scan base-package="org.yaen.starter.web" />

	<!-- mvc request/response, only in this file -->
	<mvc:annotation-driven>
		<mvc:message-converters>
			<bean class="com.alibaba.fastjson.support.spring.FastJsonHttpMessageConverter" />
		</mvc:message-converters>
	</mvc:annotation-driven>

	<!-- default home page, need controller to handle this -->
	<mvc:view-controller path="/" view-name="index.html" />

	<!-- resource for css/js/image, enable cache -->
	<mvc:resources mapping="/resources/**" location="/resources/" />

	<!-- multi part resolver -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- 设置上传文件的最大尺寸为10MB -->
		<property name="maxUploadSize">
			<value>10485760</value>
		</property>
		<property name="maxInMemorySize">
			<value>4096</value>
		</property>
	</bean>

	<!-- interceptors in order -->
	<mvc:interceptors>
		<!-- session interceptor, may use different repository for sessions -->
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<mvc:exclude-mapping path="/resources/**" />
			<beans:bean id="sessionInterceptor" class="org.yaen.starter.web.home.interceptors.SessionInterceptor">
			</beans:bean>
		</mvc:interceptor>

		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<mvc:exclude-mapping path="/resources/**" />
			<beans:bean id="weixinAuthInterceptor" class="org.yaen.starter.web.home.interceptors.StarterInterceptor">
			</beans:bean>
		</mvc:interceptor>
	</mvc:interceptors>

</beans>